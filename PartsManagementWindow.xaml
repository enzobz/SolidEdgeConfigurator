<Window x:Class="SolidEdgeConfigurator.PartsManagementWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <DataGrid x:Name="PartsDataGrid" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Part Name" Binding="{Binding PartName}" />
                <DataGridTextColumn Header="Part Number" Binding="{Binding PartNumber}" />
                <DataGridTextColumn Header="Component" Binding="{Binding Component}" />
                <DataGridTextColumn Header="Price" Binding="{Binding Price}" />
                <DataGridTextColumn Header="Qty" Binding="{Binding Qty}" />
                <DataGridTextColumn Header="Supplier" Binding="{Binding Supplier}" />
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Delete Selected" Name="DeleteSelectedButton" />
            <Button Content="Import from Assembly" Name="ImportFromAssemblyButton" />
            <Button Content="Match & Fill Info" Name="MatchFillInfoButton" />
            <Button Content="Export BOM" Name="ExportBOMButton" />
            <Button Content="Save Changes" Name="SaveChangesButton" />
        </StackPanel>
    </Grid>
</Window>